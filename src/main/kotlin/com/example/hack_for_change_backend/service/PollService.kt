//package com.example.hack_for_change_backend.service
//
//import org.springframework.stereotype.Service
//
//@Service
//class PollService (val pollRepo: PollRepo, val eventService: EventService) {
//
////    fun createPoll()
//
//    fun findPollById(id: Long) = pollRepo.findById(id).orElseThrow { NoSuchElementException("Poll with ID: $id does not exist") }
//
////    fun createPoll(eventID: Long): Poll {
////        val poll = Poll
////        pollRepo.save()
////    }
//}